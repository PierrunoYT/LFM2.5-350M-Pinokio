module.exports = {
  run: [
    {
      method: "notify",
      params: {
        html: "Installing LFM2.5-350M Chat…"
      }
    },
    {
      method: "notify",
      params: {
        html: "✅ Installed! No dependencies needed — the model downloads in-browser on first use via WebGPU."
      }
    }
  ]
}
